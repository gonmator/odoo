<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="estate_property_offer_view_tree" model="ir.ui.view">
        <field name="name">estate.property.offer.tree</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <tree string="Offers" editable="top" decoration-danger="status == 'refused'"
                  decoration-success="status == 'accepted'">
                <field name="price"/>
                <field name="partner_id"/>
                <field name="validity" string="Validity (days)"/>
                <field name="date_deadline" string="Deadline"/>
                <button name="action_accept" type="object" icon="fa-check" string="Accept"
                        attrs="{'invisible': [('status','in',['accepted', 'refused'])]}"/>
                <button name="action_refuse" type="object" icon="fa-remove" string="Refuse"
                        attrs="{'invisible': [('status','in',['accepted', 'refused'])]}"/>
                <field name="status" invisible="true"/>
            </tree>
        </field>
    </record>

    <record id="estate_property_tag_view_form" model="ir.ui.view">
        <field name="name">estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="Property Offer">
                <sheet>
                    <group col="2">
                        <field name="price"/>
                        <field name="partner_id"/>
                        <field name="validity" string="Validity (days)"/>
                        <field name="date_deadline" string="Deadline"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>